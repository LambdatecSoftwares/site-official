import styled from 'styled-components';

export const Container = styled.div`
    position: absolute;
    width: 75%;
    height: 80vh;
    top: 0;
    img {
    width: 100%;
    height: 100%;
    pointer-events: none;
    object-fit: cover;
  }
`;
